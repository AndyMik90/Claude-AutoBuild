# Настройка русского языка для Auto-Claude

## Быстрая настройка

Чтобы Auto-Claude генерировал весь контент на русском языке, выполните одно из следующих действий:

### Вариант 1: Переменная окружения (самый простой)

```bash
export AUTO_CLAUDE_LANGUAGE=ru
```

Добавьте эту строку в ваш `.bashrc`, `.zshrc` или `.env` файл для постоянного эффекта.

### Вариант 2: Конфигурационный файл проекта

Создайте файл `.auto-claude/config.json` в корне вашего проекта:

```bash
mkdir -p .auto-claude
cat > .auto-claude/config.json << 'EOF'
{
  "output_language": "ru"
}
EOF
```

### Вариант 3: Глобальная настройка

Для применения ко всем проектам:

```bash
mkdir -p ~/.auto-claude
cat > ~/.auto-claude/config.json << 'EOF'
{
  "output_language": "ru"
}
EOF
```

## Проверка настройки

Проверьте, что настройка применилась:

```bash
cd apps/backend
python3 -c "from core.language_config import get_output_language; print('Текущий язык:', get_output_language())"
```

Должно вывести: `Текущий язык: ru`

## Что изменится

После настройки, все генерируемые LLM тексты будут на русском:

- 📋 **Roadmap**: описания фич, фазы, milestone
- 📝 **Spec**: описания задач и подзадач
- 💡 **Ideation**: идеи улучшений, описания проблем
- ✅ **QA**: отчёты о проверке качества
- 📊 **Планирование**: rationale, acceptance criteria

## Примеры использования

### Создание roadmap на русском

```bash
export AUTO_CLAUDE_LANGUAGE=ru
python apps/backend/runners/roadmap_runner.py --project /path/to/project
```

### Создание spec на русском

```bash
export AUTO_CLAUDE_LANGUAGE=ru
python apps/backend/cli/main.py spec create "Добавить аутентификацию"
```

### Генерация идей на русском

```bash
export AUTO_CLAUDE_LANGUAGE=ru
python apps/backend/runners/ideation_runner.py --project /path/to/project
```

## Отключение (возврат к английскому)

Чтобы вернуться к английскому языку:

```bash
unset AUTO_CLAUDE_LANGUAGE
# или
export AUTO_CLAUDE_LANGUAGE=en
```

Или удалите/измените файл `.auto-claude/config.json`.

## Технические детали

- Инструкция о языке автоматически добавляется в начало каждого промпта
- Технические идентификаторы (id, имена файлов, код) остаются на английском
- Настройка применяется ко всем агентам: planner, coder, roadmap, ideation, QA
- Не требует изменения markdown файлов промптов

## Поддержка других языков

Помимо русского, поддерживаются:
- `en` - English (по умолчанию)
- `es` - Español
- `de` - Deutsch
- `fr` - Français

Для добавления нового языка см. [LANGUAGE_CONFIGURATION.md](./LANGUAGE_CONFIGURATION.md)
