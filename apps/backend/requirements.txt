# Auto-Build Framework Dependencies
claude-agent-sdk>=0.1.16
python-dotenv>=1.0.0

# TOML parsing fallback for Python < 3.11
tomli>=2.0.0; python_version < "3.11"

# Memory Integration - LadybugDB (embedded graph database)
# Requires Python 3.12+ and cmake (for building C extensions)
#
# To install cmake:
#   macOS: brew install cmake
#   Ubuntu/Debian: sudo apt-get install cmake
#   Windows: choco install cmake OR download from cmake.org
#
# If you get build errors, you can disable Graphiti by setting GRAPHITI_ENABLED=false
# in your .env file. Auto-Claude will work without memory features.
real_ladybug>=0.13.0; python_version >= "3.12"
graphiti-core>=0.5.0; python_version >= "3.12"

# Google AI (optional - for Gemini LLM and embeddings)
google-generativeai>=0.8.0

# Google Cloud Vertex AI (optional - for Claude on GCP)
google-auth>=2.23.0

# Pydantic for structured output schemas
pydantic>=2.0.0
